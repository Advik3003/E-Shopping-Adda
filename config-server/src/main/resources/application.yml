server:
  port: 8888

spring:
  application:
    name: config-server

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: file:///D:/SpringBootWorkSpace/other-projects/E-Shopping-Adda/config-repo
  boot:
    admin:
      client:
        url: http://localhost:9090

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: "*"
